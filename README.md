# React + TypeScript + Vite

Run `npm run install` and `npm run dev` to start an app

**Features**

- Create new notes at any position on the board.
- Drag and resize notes with mouse interactions.
- Delete notes by dragging them to a trash zone.
- Notes automatically bring to front when focused.
- Customizable note size with minimum and maximum constraints.